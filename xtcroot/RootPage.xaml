<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="xtcroot.RootPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:xtcroot"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Margin="60">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="0" HorizontalAlignment="Stretch" VerticalAlignment="Top" Height="100" TextAlignment="Start">
                设备信息:
        </TextBlock>
        <Button Grid.Row="1" Content="刷新设备" Margin="5" HorizontalAlignment="Stretch" VerticalAlignment="Top"/>
        <Button Grid.Row="2" Content="开始root" Margin="5" HorizontalAlignment="Stretch" VerticalAlignment="Top"/>
        <ProgressBar Grid.Row="3" VerticalAlignment="Bottom" HorizontalAlignment="Stretch" Margin="0,10"/>
    </Grid>
</Page>
